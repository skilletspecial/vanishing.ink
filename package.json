{
  "name": "vanishing-ink",
  "version": "1.0.0",
  "type": "module",
  "description": "Self-destructing, end-to-end encrypted secret sharing",
  "scripts": {
    "dev": "bun run --hot server/index.ts",
    "start": "bun run server/index.ts",
    "prepare": "husky"
  },
  "dependencies": {
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7"
  }
}
